# Local profile policy. Authentication data is never bundled.
- id: llm-subscriptions
  config:
    providers: [codex, claude]
- id: automations
  config:
    maxConcurrentRuns: 2
    allowedProjectRoots:
      - "~"
